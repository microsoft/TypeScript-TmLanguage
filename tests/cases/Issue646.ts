// Good
const foo = 5; // The json data that the extension sent
console.log(`${foo}`)

// Good
const foo = 5
console.log(`${foo}`)

// Bad
const foo = 5 // The json data that the extension sent
console.log(`${foo}`)