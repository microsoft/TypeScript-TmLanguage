'use strict';

        var pieces = [];
        var numeration = [];

        for (var i in pieces) {
            switch (true) {
                case /^\"?Accession\"?/.test(pieces[i]):
                    numeration[0] = i;
                    break;
                case /^\"?Accession\"?/.test(pieces[i]):
                    numeration[0] = i;
                    break;
                case /^\"?Accession\"?/.test(pieces[i]):
                    numeration[0] = i;
                    break;
                case /^\"?Accession\"?/.test(pieces[i]):
                    numeration[0] = i;
                    break;
                case /^\"?Accession\"?/.test(pieces[i]):
                    numeration[0] = i;
                    break;
                case /^\"?Accession\"?/.test(pieces[i]):
                    numeration[0] = i;
                    break;
                case /^\"?Accession\"?/.test(pieces[i]):
                    numeration[0] = i;
                    break;
                case /^\"?Accession\"?/.test(pieces[i]):
                    numeration[0] = i;
                    break;
                case /^\"?Accession\"?/.test(pieces[i]):
                    numeration[0] = i;
                    break;
                case /^\"?Accession\"?/.test(pieces[i]):
                    numeration[0] = i;
                    break;
                case /^\"?Accession\"?/.test(pieces[i]):
                    numeration[0] = i;
                    break;
                case /^\"?Accession\"?/.test(pieces[i]):
                    numeration[0] = i;
                    break;
                case /^\"?Accession\"?/.test(pieces[i]):
                    numeration[0] = i;
                    break;
                case /^\"?Accession\"?/.test(pieces[i]):
                    numeration[0] = i;
                    break;
                case /^\"?Accession\"?/.test(pieces[i]):
                    numeration[0] = i;
                    break;
                case /^\"?Accession\"?/.test(pieces[i]):
                    numeration[0] = i;
                    break;
                case /^\"?Accession\"?/.test(pieces[i]):
                    numeration[0] = i;
                    break;
                case /^\"?Accession\"?/.test(pieces[i]):
                    numeration[0] = i;
                    break;
                case /^\"?Accession\"?/.test(pieces[i]):
                    numeration[0] = i;
                    break;
                case /^\"?Accession\"?/.test(pieces[i]):
                    numeration[0] = i;
                    break;
                case /^\"?Accession\"?/.test(pieces[i]):
                    numeration[0] = i;
                    break;
                case /^\"?Accession\"?/.test(pieces[i]):
                    numeration[0] = i;
                    break;
                case /^\"?Accession\"?/.test(pieces[i]):
                    numeration[0] = i;
                    break;
                case /^\"?Accession\"?/.test(pieces[i]):
                    numeration[0] = i;
                    break;
                case /^\"?Accession\"?/.test(pieces[i]):
                    numeration[0] = i;
                    break;
                case /^\"?Accession\"?/.test(pieces[i]):
                    numeration[0] = i;
                    break;
                case /^\"?Accession\"?/.test(pieces[i]):
                    numeration[0] = i;
                    break;
                case /^\"?Accession\"?/.test(pieces[i]):
                    numeration[0] = i;
                    break;
                case /^\"?Accession\"?/.test(pieces[i]):
                    numeration[0] = i;
                    break;
                case /^\"?Accession\"?/.test(pieces[i]):
                    numeration[0] = i;
                    break;
                case /^\"?Accession\"?/.test(pieces[i]):
                    numeration[0] = i;
                    break;
                case /^\"?Accession\"?/.test(pieces[i]):
                    numeration[0] = i;
                    break;
                case /^\"?Accession\"?/.test(pieces[i]):
                    numeration[0] = i;
                    break;
            }
        }
