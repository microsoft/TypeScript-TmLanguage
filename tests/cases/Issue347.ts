const a = new Promise (resolve => {
    const file = this.props.doc.file;
    resolve({
        abc: Math.random()
    });

    class Foo extends React.Component {
        render() {
        }
    }
    
});