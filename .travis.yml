language: node_js

node_js:
  - 'stable'
  
sudo: false

env:
  - workerCount=2

matrix:
  fast_finish: true

branches:
  only:
  - master

install:
  - npm install

git:
  depth: 1
